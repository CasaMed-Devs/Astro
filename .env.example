# Public config for the Expo app. EXPO_PUBLIC_* variables are bundled into
# the client and visible to anyone — never put secrets here.

# Local backend URL while developing (see functions/.env.example for the
# backend's own secrets). Point this at your deployed Cloud Function URL
# for production builds.
EXPO_PUBLIC_API_URL=http://localhost:3000
